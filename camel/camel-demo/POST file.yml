info:
  name: POST file
  type: http

http:
  method: POST
  url: http://localhost:8080/file
  body:
    type: text
    data: move
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
